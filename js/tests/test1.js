"use strict";
define([
    'Logger'
], function(Logger){
     describe("Logger module", function () {
        it("loaded module", function() {
            expect(Logger).toBeDefined();
            expect(Logger)
        });
    });
});